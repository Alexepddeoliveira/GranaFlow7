<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="com.alex.granaflow7">

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:roundIcon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/Theme.GranaFlow7"
        tools:targetApi="31">
        <activity
            android:name=".ChartActivity"
            android:exported="false" />
        <!-- Tela Sobre -->
        <activity
            android:name=".AboutActivity"
            android:exported="false" /> <!-- Tela de Resumo -->
        <activity
            android:name=".SummaryActivity"
            android:exported="false" /> <!-- Tela que mostra o Fragment -->
        <activity
            android:name=".ListActivity"
            android:exported="false" /> <!-- Tela de adicionar lançamento -->
        <activity
            android:name=".AddTransactionActivity"
            android:exported="false" /> <!-- Tela inicial -->
        <activity
            android:name=".MainActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>